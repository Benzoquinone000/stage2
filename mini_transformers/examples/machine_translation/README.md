# Machine Translation

This folder will contain Transformer encoder-decoder translation experiments.

Run the tiny smoke test:

```bash
python examples/machine_translation/train_transformer_mt.py --epochs 1
```
